# =============================================================================
# DPolG Buchungssystem - Umgebungskonfiguration
# =============================================================================
# Kopiere diese Datei zu .env und konfiguriere die Werte.
# NIEMALS .env in Git committen!
#
# MULTI-DEVICE SETUP:
# - Windows: copy .env.example .env
# - macOS:   cp .env.example .env
# - Jedes Gerät hat seine eigene .env Datei (bleibt lokal!)

# =============================================================================
# DATENBANK
# =============================================================================

# PostgreSQL Connection String
# Format: postgres://user:password@host:port/database
#
# Port 6432 = pgBouncer (empfohlen für Connection Pooling)
# Port 5432 = Direkter PostgreSQL Zugang (nur für Admin)
DATABASE_URL=postgres://dpolg_admin:DEIN_PASSWORT@141.147.3.123:6432/dpolg_booking

# =============================================================================
# UMGEBUNG
# =============================================================================

# Optionen: development, production
# development = Debug-Logging, DevTools aktiviert
# production  = Optimiert, keine Debug-Features
ENVIRONMENT=development

# =============================================================================
# OPTIONAL: Connection Pool (normalerweise nicht ändern)
# =============================================================================

# Maximum Connections im Pool (Standard: 20)
# DB_POOL_MAX_SIZE=20

# Connection Timeout in Sekunden (Standard: 30)
# DB_POOL_TIMEOUT_SECONDS=30

# =============================================================================
# SICHERHEITSHINWEISE
# =============================================================================
# - NIEMALS .env in Git committen (bereits in .gitignore)
# - GitHub Secrets für CI/CD verwenden
# - Passwörter regelmäßig rotieren
