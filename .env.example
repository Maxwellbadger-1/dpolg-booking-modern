# PostgreSQL Database Configuration
# Copy this file to .env and fill in your values

# Database Connection URL
# Format: postgres://user:password@host:port/database
DATABASE_URL=postgres://dpolg_admin:YOUR_PASSWORD@141.147.3.123:6432/dpolg_booking

# Environment (development, staging, production)
ENVIRONMENT=development

# Optional: Connection Pool Settings (handled by deadpool-postgres)
# DB_POOL_MAX_SIZE=20
# DB_POOL_TIMEOUT_SECONDS=30

# Notes:
# - Port 6432 is pgBouncer (connection pooling)
# - Port 5432 is direct PostgreSQL (avoid for production)
# - Use pgBouncer for better multi-user performance
# - Password should be kept secret (never commit .env to git!)
